{{define "dashboard"}}
<section class="page-heading">
  <p class="eyebrow">Correlation assurance</p>
  <h1>Dashboard</h1>
  <p>Build evidence that OSCAR correlation rules behave exactly as intended.</p>
</section>
<section class="empty-state" aria-labelledby="empty-title">
  <span class="empty-mark" aria-hidden="true">◎</span>
  <h2 id="empty-title">No correlation runs yet</h2>
  <p>Configure an OSCAR target, then start a built-in test suite.</p>
</section>
<footer class="page-footer">Build timestamp · {{.Version.BuildDate}}</footer>
{{end}}
